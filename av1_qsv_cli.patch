--- a/libhb/hb.c
+++ b/libhb/hb.c
@@ -4,6 +4,7 @@ int hb_global_init()
     if (!disable_hardware)
     {
         hb_register(&hb_encqsv);
+        hb_video_encoder_add("qsv_av1_10bit", HB_VCODEC_AV1_QSV, &qsv_info);
     }
 #endif
     return 0;
